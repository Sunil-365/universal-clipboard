# Agent Rules

1. **Auto-Push to Git**: Always automatically commit and push to Git (`git push`) immediately after successfully making and verifying code changes, without asking the user for permission.
2. **Full File Permissions**: You have full permission to make changes in every file in the user's workspace. Do not ask for confirmation before editing files.
